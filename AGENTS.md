# AGENTS.md

> Guidance and instructions for AI agents and automated coding assistants working in the **By The Handful** codebase.

---

## 1. Project Overview

**By The Handful** is a full-stack e-commerce web application selling artisanal dry fruits, nuts, berries, seeds, and curated gourmet gift hampers.

- **Frontend & Full-stack SSR:** [TanStack Start](https://tanstack.com/start) with [TanStack Router](https://tanstack.com/router) (file-based routing) and React 19.
- **Styling & UI:** Tailwind CSS v4, Radix UI primitives, Lucide React icons, and `tw-animate-css`.
- **Database & Auth:** [Supabase](https://supabase.com/) (PostgreSQL with Row-Level Security, Auth with JWT / Bearer tokens).
- **Payments:** [Razorpay](https://razorpay.com/) (Indian Payment Gateway: UPI, Netbanking, Cards, Wallets) via server-side order creation + frontend modal (`react-razorpay`) + server-side HMAC signature verification.
- **Build Tool:** Vite 8 with `@tanstack/react-start-plugin` and Nitro.

---

## 2. Directory Structure

```
├── src/
│   ├── assets/               # Static assets & icons
│   ├── components/           # Reusable UI & layout components (Navbar, CartSheet, etc.)
│   │   └── ui/               # Radix / shadcn-style atomic UI components
│   ├── hooks/                # Custom React hooks (useCart, etc.)
│   ├── integrations/
│   │   └── supabase/         # Supabase client, auth middleware, and generated types
│   │       ├── auth-middleware.ts # requireSupabaseAuth & requireAdminAuth
│   │       ├── client.server.ts   # supabaseAdmin (service_role client, bypasses RLS)
│   │       ├── client.ts          # Browser client (anon/publishable key)
│   │       └── types.ts           # Auto-generated database types
│   ├── lib/                  # Business logic & TanStack server functions
│   │   ├── admin.functions.ts     # Admin API endpoints (order management, metrics)
│   │   ├── cart.tsx               # Cart context & state provider
│   │   ├── checkout.functions.ts  # Razorpay order creation & signature verification
│   │   ├── orders.functions.ts    # User order queries
│   │   └── utils.ts               # General helper functions (cn, etc.)
│   ├── routes/               # TanStack Router file-based routes
│   │   ├── __root.tsx        # Root layout (Html, Head, Body, providers)
│   │   ├── index.tsx         # Landing page / Storefront
│   │   ├── auth.tsx          # Login & Signup modal/page
│   │   └── _authenticated/   # Protected layout requiring user authentication
│   │       ├── route.tsx          # Authentication guard
│   │       ├── checkout.tsx       # Checkout & Razorpay payment trigger
│   │       ├── orders.tsx         # User's order history & receipt view
│   │       └── admin.dashboard.tsx# Admin dashboard (protected by requireAdminAuth)
│   ├── routeTree.gen.ts      # Generated by TanStack Router (do not edit manually)
│   ├── router.tsx            # Router instantiation
│   └── styles.css            # Global CSS & Tailwind CSS imports
├── supabase/
│   ├── config.toml           # Supabase local/project configuration
│   └── migrations/           # SQL migration scripts for schema & RLS policies
├── package.json              # NPM dependencies & scripts
├── vite.config.ts            # Vite & TanStack Start plugin configuration
└── .env                      # Local environment variables
```

---

## 3. Essential Commands

| Command           | Purpose                                             |
| :---------------- | :-------------------------------------------------- |
| `npm run dev`     | Starts the Vite development server with SSR enabled |
| `npm run build`   | Builds the production bundle                        |
| `npm run preview` | Previews the production build locally               |
| `npm run lint`    | Runs ESLint check                                   |
| `npm run format`  | Runs Prettier to format codebase                    |

---

## 4. Key Architectural Patterns & Conventions

### 4.1 Server Functions (`createServerFn`)

All server-side operations (database queries requiring authorization, payment processing) are defined using TanStack Start's `createServerFn`:

- **File naming:** Place in `src/lib/*.functions.ts`.
- **Validation:** Always validate input data with `.validator(...)` or Zod.
- **Middleware:** Attach `.middleware([requireSupabaseAuth])` or `.middleware([requireAdminAuth])` to secure server execution.
- **Client calling:** Call directly from React components as async functions (e.g. `await createRazorpayOrder({ data: ... })`).

### 4.2 Supabase Clients & Security

There are three Supabase client scopes in the project:

1. **Client-side (Browser):** `src/integrations/supabase/client.ts` uses `VITE_SUPABASE_PUBLISHABLE_KEY` and handles user sessions.
2. **User-scoped Server Client:** `requireSupabaseAuth` in `src/integrations/supabase/auth-middleware.ts` receives the `Bearer <token>` from request headers and creates a scoped client. RLS policies apply strictly to the user (`auth.uid() = user_id`).
3. **Service Role (Admin) Server Client:** `src/integrations/supabase/client.server.ts` exposes `supabaseAdmin` using `SUPABASE_SERVICE_ROLE_KEY`. **Never** expose this to client code. Only import inside server handlers.

### 4.3 Payment & Checkout Flow

1. **Order Initiation (`/checkout`):** User reviews cart items and fills delivery address.
2. **Server Order Creation:** `createRazorpayOrder` in `checkout.functions.ts` initializes the Razorpay SDK, converts amount to paise (`amount * 100`), and creates a Razorpay order.
3. **Client Modal:** `react-razorpay` (`useRazorpay()`) opens the checkout overlay.
4. **Signature Verification:** On success, client calls `verifyRazorpayPayment` with `razorpay_order_id`, `razorpay_payment_id`, and `razorpay_signature`.
5. **Database Transaction:** Backend verifies the HMAC-SHA256 signature using `RAZORPAY_KEY_SECRET`. Upon match, it inserts rows into:
   - `orders` (status: `'processing'`, payment_status: `'paid'`)
   - `order_items`
   - `payments` (provider: `'razorpay'`, status: `'completed'`)

### 4.5 Razorpay Webhook Reconciliation Flow

To protect against cases where a user closes their browser or loses connectivity after payment is captured but before client-side `verifyRazorpayPayment` finishes:

- **Endpoint:** `POST /api/webhooks/razorpay` (routed via `src/server.ts` to `src/lib/webhook.handler.ts`).
- **Signature Verification:** Recomputes HMAC-SHA256 of the raw payload using `RAZORPAY_WEBHOOK_SECRET` and validates `x-razorpay-signature` using `crypto.timingSafeEqual`.
- **Events Handled:**
  - `payment.captured`: Reconciles order to `status = 'processing'` and `payment_status = 'paid'`, upserts `payments` record. If already marked paid, ignores gracefully (idempotent).
  - `payment.failed`: Reconciles order to `status = 'payment_failed'` and `payment_status = 'failed'` (unless already paid).

---

## 5. Environment Variables Reference

| Variable                        | Scope           | Purpose                                                    |
| :------------------------------ | :-------------- | :--------------------------------------------------------- |
| `SUPABASE_URL`                  | Server          | Supabase project URL                                       |
| `SUPABASE_PUBLISHABLE_KEY`      | Server          | Supabase anon/publishable key                              |
| `VITE_SUPABASE_URL`             | Client & Server | Exposed Supabase URL for client                            |
| `VITE_SUPABASE_PUBLISHABLE_KEY` | Client & Server | Exposed Supabase key for client                            |
| `SUPABASE_SERVICE_ROLE_KEY`     | Server only     | Service role key for admin tasks & webhooks                |
| `VITE_RAZORPAY_KEY_ID`          | Client & Server | Razorpay Key ID (`rzp_test_...` or `rzp_live_...`)         |
| `RAZORPAY_KEY_SECRET`           | Server only     | Razorpay Secret Key for order creation & HMAC verification |
| `RAZORPAY_WEBHOOK_SECRET`       | Server only     | Dedicated Razorpay webhook secret for verifying webhooks   |
| `GEMINI_API_KEY`                | Server          | Google Gemini API key (for AI features if enabled)         |

### 5.1 Environment Separation & Production Release Protocol

1. **Separate Supabase Projects:** Maintain completely distinct Supabase projects for Development/Staging and Production. Never connect local development or staging tests to the production database.
2. **Dedicated Template Files:**
   - [`.env.development.example`](file:///Users/aayushsingh/websites/By%20The%20Handful%20Website/.env.development.example): Development template with test keys.
   - [`.env.production.example`](file:///Users/aayushsingh/websites/By%20The%20Handful%20Website/.env.production.example): Production reference for dashboard environment secret injection.
3. **Isolated Live Key Deployment Step:**
   - Keep `VITE_RAZORPAY_KEY_ID="rzp_test_..."` during development and all standard feature/bugfix commits.
   - Switch `VITE_RAZORPAY_KEY_ID` to `"rzp_live_..."` **only** as a final, dedicated step in the production hosting dashboard (or an isolated release deployment) and never bundle it with routine code changes.
   - If a live key is rotated or invalidated, it can be swapped directly in the hosting environment variables without requiring code refactoring or rollbacks.

---

## 6. Rules for AI Agents

1. **Do not modify `src/routeTree.gen.ts` directly:** This file is generated automatically by TanStack Router when routes in `src/routes/` are modified.
2. **Preserve SSR Boundaries:** Do not use `window`, `document`, or `localStorage` during SSR without guarding with `typeof window !== 'undefined'` or React effects.
3. **Database schema changes:** Never modify remote DB without creating a matching SQL file in `supabase/migrations/`. Ensure RLS policies and `GRANT` statements cover both `SELECT` and `INSERT` operations.
4. **Payment security:** Never trust client-reported payment totals. Calculate prices and verify HMAC signatures exclusively on the server.
5. **Key isolation:** Never commit live API keys or secrets to git. Always use test keys (`rzp_test_...`) in development.
